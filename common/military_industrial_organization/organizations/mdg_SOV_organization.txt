SOV_tupolev_design_bureau_organization_boat = {
	include = generic_small_ship_organization
	icon = GFX_idea_tupolev_design_bureau

	allowed = {
		original_tag = SOV
	}

	available = {
		# When in a Foreign MIO, countries need to be at peace with original country
		IF = {
			limit = {
				FROM = { NOT = { original_tag = SOV } }
			}
			FROM = { NOT = { has_war_with = SOV } }
		}
		ELSE = { # original_tag = SOV
			FROM = { controls_state = 219 } # Moskow
		}

		# IF = {
		# 	custom_trigger_tooltip = {
		# 		tooltip = has_invited_mio_tt
		# 		FROM = {
		# 			has_country_flag = has_invited_crda_mio_flag
		# 		}
		# 	}
		# }
	}

	add_trait = {
		token = generic_mio_trait_fast_boats
		name = generic_mio_trait_fast_boats
		icon = GFX_generic_mio_trait_icon_maximum_speed
		special_trait_background = yes

		position = { x = 8 y = 0 } 
		production_bonus = {
			production_cost_factor = -0.15
		}
		equipment_bonus = {
			build_cost_ic = -0.05
			naval_speed = 0.15
			max_strength = -0.15
			naval_weather_penalty_factor = 0.2
			naval_range = -0.1
		}
		ai_will_do = {
			base = 2
		}
	}
}

SOV_ccb5_organization = {
	include = generic_small_ship_organization
	icon = GFX_idea_SOV_ccb5

	allowed = {
		original_tag = SOV
	}

	available = {
		# When in a Foreign MIO, countries need to be at peace with original country
		IF = {
			limit = {
				FROM = { NOT = { original_tag = SOV } }
			}
			FROM = { NOT = { has_war_with = SOV } }
		}
		ELSE = { # original_tag = SOV
			FROM = { controls_state = 252 } # Nijniy Novgorod/Gorkiy
		}

		# IF = {
		# 	custom_trigger_tooltip = {
		# 		tooltip = has_invited_mio_tt
		# 		FROM = {
		# 			has_country_flag = has_invited_crda_mio_flag
		# 		}
		# 	}
		# }
	}
	# Nothing special, all stuff is generic
}
