# AI Ship Designs for GER

GER_mdg_frigates = {
	category = naval

	blocked_for = {
	}

	available_for = {
		GER
	}

	roles = {
		naval_screen_frigate
	}

	priority = {
		factor = 50
		modifier = {
			factor = 1
		}
	}

	mdg_frigate_early = { # Type 1934 adaptation — light cannon, fire control, torpedo, depth charge

		priority = {
			factor = 1
			modifier = {
				factor = 1
			}
		}

		role_icon_index = 2

		target_variant = {
			match_value = 2000.0

			type = mdg_ship_hull_frigate_1
			modules = {
				fixed_ship_battery_slot = {
					any_of = { mdg_ship_light_cannon }
					upgrade > current
				}
				fixed_ship_anti_air_slot = {
					any_of = { ship_anti_air }
					upgrade > current
				}
				fixed_ship_fire_control_system_slot = {
					any_of = { ship_fire_control_system }
					upgrade > current
				}
				fixed_ship_radar_slot = empty
				fixed_ship_engine_slot = {
					any_of = { light_ship_engine }
					upgrade > current
				}
				fixed_ship_torpedo_slot = {
					any_of = { ship_torpedo }
					upgrade > current
				}
				mid_1_custom_slot = {
					any_of = { ship_depth_charge }
					upgrade > current
				}
				rear_1_custom_slot = {
					any_of = { ship_depth_charge }
					upgrade > current
				}
			}
		}

		allowed_modules = {
			ship_depth_charge
			mdg_ship_light_cannon
			mdg_ship_light_cannon_dp
			ship_torpedo
			ship_anti_air
			light_ship_engine
			ship_sonar
			ship_fire_control_system
			ship_radar
		}
	}

	mdg_frigate_basic = { # Type 1936 adaptation — light cannon, torpedo, depth charge

		priority = {
			factor = 50
		}

		role_icon_index = 2

		target_variant = {
			match_value = 2500.0

			type = mdg_ship_hull_frigate_2
			modules = {
				fixed_ship_battery_slot = {
					any_of = { mdg_ship_light_cannon }
					upgrade > current
				}
				fixed_ship_anti_air_slot = {
					any_of = { ship_anti_air }
					upgrade > current
				}
				fixed_ship_fire_control_system_slot = {
					any_of = { ship_fire_control_system }
					upgrade > current
				}
				fixed_ship_radar_slot = empty
				fixed_ship_engine_slot = {
					any_of = { light_ship_engine }
					upgrade > current
				}
				fixed_ship_torpedo_slot = {
					any_of = { ship_torpedo }
					upgrade > current
				}
				mid_1_custom_slot = {
					any_of = { ship_depth_charge }
					upgrade > current
				}
				rear_1_custom_slot = {
					any_of = { ship_depth_charge }
					upgrade > current
				}
			}
		}

		allowed_modules = {
			ship_anti_air
			mdg_ship_light_cannon
			mdg_ship_light_cannon_dp
			ship_torpedo
			ship_depth_charge
			light_ship_engine
			ship_fire_control_system
			ship_radar
			ship_sonar
		}
	}

	mdg_frigate_improved = { # Type 1936A adaptation — light cannon, sonar, torpedo, front+mid AA

		priority = {
			factor = 5
			modifier = {
				factor = 1
			}
		}

		role_icon_index = 2

		target_variant = {
			match_value = 3500.0

			type = mdg_ship_hull_frigate_3
			modules = {
				fixed_ship_battery_slot = {
					any_of = { mdg_ship_light_cannon mdg_ship_light_cannon_dp }
					upgrade > current
				}
				fixed_ship_anti_air_slot = {
					any_of = { ship_anti_air }
					upgrade > current
				}
				fixed_ship_fire_control_system_slot = {
					any_of = { ship_fire_control_system }
					upgrade > current
				}
				fixed_ship_radar_slot = {
					any_of = { ship_sonar }
					upgrade > current
				}
				fixed_ship_engine_slot = {
					any_of = { light_ship_engine }
					upgrade > current
				}
				fixed_ship_torpedo_slot = {
					any_of = { ship_torpedo }
					upgrade > current
				}
				front_1_custom_slot = {
					any_of = { ship_anti_air }
					upgrade > current
				}
				mid_1_custom_slot = {
					any_of = { ship_anti_air }
					upgrade > current
				}
				rear_1_custom_slot = {
					any_of = { ship_depth_charge }
					upgrade > current
				}
			}
		}

		allowed_modules = {
			ship_anti_air
			mdg_ship_light_cannon
			mdg_ship_light_cannon_dp
			ship_torpedo
			ship_depth_charge
			light_ship_engine
			ship_fire_control_system
			ship_radar
			ship_sonar
		}
	}

	mdg_frigate_advanced = { # Type 1936B adaptation — light cannon, torpedo, heavy AA, extra battery mid

		priority = {
			factor = 1
			modifier = {
				factor = 20
				GER_naval_interest_trigger = yes
			}
		}

		role_icon_index = 2

		target_variant = {
			match_value = 3500.0

			type = mdg_ship_hull_frigate_4
			modules = {
				fixed_ship_battery_slot = {
					any_of = { mdg_ship_light_cannon mdg_ship_light_cannon_dp }
					upgrade > current
				}
				fixed_ship_anti_air_slot = {
					any_of = { ship_anti_air }
					upgrade > current
				}
				fixed_ship_fire_control_system_slot = {
					any_of = { ship_radar }
					upgrade > current
				}
				fixed_ship_radar_slot = {
					any_of = { ship_sonar }
					upgrade > current
				}
				fixed_ship_engine_slot = {
					any_of = { light_ship_engine }
					upgrade > current
				}
				fixed_ship_torpedo_slot = {
					any_of = { ship_torpedo }
					upgrade > current
				}
				front_1_custom_slot = {
					any_of = { ship_anti_air }
					upgrade > current
				}
				mid_1_custom_slot = {
					any_of = { ship_anti_air }
					upgrade > current
				}
				mid_2_custom_slot = {
					any_of = { mdg_ship_light_cannon mdg_ship_light_cannon_dp }
					upgrade > current
				}
				rear_1_custom_slot = {
					any_of = { ship_depth_charge }
					upgrade > current
				}
			}
		}

		allowed_modules = {
			ship_anti_air
			mdg_ship_light_cannon
			mdg_ship_light_cannon_dp
			ship_torpedo
			ship_depth_charge
			light_ship_engine
			ship_fire_control_system
			ship_radar
			ship_sonar
		}
	}
}

GER_escorts_frigate = {
	category = naval

	blocked_for = {
	}

	available_for = {
		GER
	}

	roles = {
		naval_escort_frigate
	}

	priority = {
		factor = 100
		modifier = {
			factor = 2
			anti_submarine_strategy_required_trigger = yes
		}
	}

	escort_early_frigate = {

		priority = {
			factor = 10
			modifier = {
				factor = 1
			}
		}

		role_icon_index = 3

		target_variant = {
			match_value = 500.0

			type = mdg_ship_hull_frigate_1
			modules = {
				fixed_ship_battery_slot = {
					any_of = { mdg_ship_light_cannon_dp_1 }
					upgrade > current
				}
				fixed_ship_anti_air_slot = {
					any_of = { ship_anti_air }
					upgrade > current
				}
				fixed_ship_fire_control_system_slot = {
					any_of = { ship_fire_control_system }
					upgrade > current
				}
				fixed_ship_radar_slot = {
					any_of = { ship_sonar }
					upgrade > current
				}
				fixed_ship_engine_slot = {
					any_of = { light_ship_engine }
					upgrade > current
				}
				fixed_ship_torpedo_slot = empty
				mid_1_custom_slot = {
					any_of = { ship_depth_charge }
					upgrade > current
				}
				rear_1_custom_slot = {
					any_of = { ship_depth_charge }
					upgrade > current
				}
			}
		}

		allowed_modules = {
			ship_depth_charge
			ship_anti_air
			mdg_ship_light_cannon
			mdg_ship_light_cannon_dp
			ship_sonar
			ship_fire_control_system
			ship_radar
			light_ship_engine
		}
	}

	escort_basic_frigate = {

		priority = {
			factor = 50
		}

		role_icon_index = 3

		target_variant = {
			match_value = 2000.0

			type = mdg_ship_hull_frigate_2
			modules = {
				fixed_ship_battery_slot = {
					any_of = { mdg_ship_light_cannon_dp_1 }
					upgrade > current
				}
				fixed_ship_anti_air_slot = {
					any_of = { ship_anti_air }
					upgrade > current
				}
				fixed_ship_fire_control_system_slot = {
					any_of = { ship_fire_control_system }
					upgrade > current
				}
				fixed_ship_radar_slot = {
					any_of = { ship_sonar }
					upgrade > current
				}
				fixed_ship_engine_slot = {
					any_of = { light_ship_engine }
					upgrade > current
				}
				fixed_ship_torpedo_slot = empty
				mid_1_custom_slot = {
					any_of = { ship_depth_charge }
					upgrade > current
				}
				rear_1_custom_slot = {
					any_of = { ship_depth_charge }
					upgrade > current
				}
			}
		}

		allowed_modules = {
			ship_anti_air
			ship_depth_charge
			mdg_ship_light_cannon
			mdg_ship_light_cannon_dp
			ship_sonar
			ship_fire_control_system
			ship_radar
			light_ship_engine
		}
	}

	escort_improved_frigate = {

		priority = {
			factor = 5
			modifier = {
				factor = 1
			}
		}

		role_icon_index = 3

		target_variant = {
			match_value = 3000.0

			type = mdg_ship_hull_frigate_3
			modules = {
				fixed_ship_battery_slot = {
					any_of = { mdg_ship_light_cannon_dp_1 }
					upgrade > current
				}
				fixed_ship_anti_air_slot = {
					any_of = { ship_anti_air }
					upgrade > current
				}
				fixed_ship_fire_control_system_slot = {
					any_of = { ship_fire_control_system }
					upgrade > current
				}
				fixed_ship_radar_slot = {
					any_of = { ship_sonar }
					upgrade > current
				}
				fixed_ship_engine_slot = {
					any_of = { light_ship_engine }
					upgrade > current
				}
				fixed_ship_torpedo_slot = empty
				mid_1_custom_slot = {
					any_of = { ship_depth_charge }
					upgrade > current
				}
				rear_1_custom_slot = {
					any_of = { ship_depth_charge }
					upgrade > current
				}
			}
		}

		allowed_modules = {
			ship_anti_air
			ship_depth_charge
			mdg_ship_light_cannon
			mdg_ship_light_cannon_dp
			ship_sonar
			ship_fire_control_system
			ship_radar
			light_ship_engine
		}
	}

	escort_advanced_frigate = {

		priority = {
			factor = 1
			modifier = {
				factor = 20
				GER_naval_interest_trigger = yes
			}
		}

		role_icon_index = 3

		target_variant = {
			match_value = 3500.0

			type = mdg_ship_hull_frigate_4
			modules = {
				fixed_ship_battery_slot = {
					any_of = { mdg_ship_light_cannon_dp_2 }
					upgrade > current
				}
				fixed_ship_anti_air_slot = {
					any_of = { ship_anti_air }
					upgrade > current
				}
				fixed_ship_fire_control_system_slot = {
					any_of = { ship_fire_control_system }
					upgrade > current
				}
				fixed_ship_radar_slot = {
					any_of = { ship_sonar }
					upgrade > current
				}
				fixed_ship_engine_slot = {
					any_of = { light_ship_engine }
					upgrade > current
				}
				fixed_ship_torpedo_slot = empty
				mid_1_custom_slot = {
					any_of = { ship_depth_charge }
					upgrade > current
				}
				mid_2_custom_slot = {
					any_of = { ship_depth_charge }
					upgrade > current
				}
				rear_1_custom_slot = {
					any_of = { ship_anti_air }
					upgrade > current
				}
			}
		}

		allowed_modules = {
			ship_anti_air
			ship_depth_charge
			mdg_ship_light_cannon
			mdg_ship_light_cannon_dp
			ship_sonar
			ship_fire_control_system
			ship_radar
			light_ship_engine
		}
	}
}

GER_naval_mine_sweeper_frigate = {
	category = naval

	blocked_for = {
	}

	available_for = {
		GER
	}

	roles = {
		naval_mine_sweeper_frigate
	}

	priority = {
		factor = 2
		modifier = {
			factor = 5
			has_war = yes
		}
	}

	mine_sweeper_light_early_frigate = { # base variant

		priority = {
			factor = 10
			modifier = {
				factor = 1
			}
		}
		enable = { has_tech = basic_naval_mines }
		role_icon_index = 5

		target_variant = {
			match_value = 1000.0

			type = mdg_ship_hull_frigate
			modules = {
				rear_1_custom_slot = ship_mine_sweeper_1
				fixed_ship_battery_slot = {
					any_of = { mdg_ship_light_cannon_dp_2 }
					upgrade > current
				}
				fixed_ship_engine_slot = {
					any_of = { light_ship_engine }
					upgrade > current
				}
				fixed_ship_torpedo_slot = empty
			}
		}
		requirements = {
			module = ship_mine_sweeper_1
		}
		allowed_modules = {
			ship_mine_warfare
			mdg_ship_light_cannon
			mdg_ship_light_cannon_dp
			ship_anti_air
			ship_depth_charge
			ship_fire_control_system
			light_ship_engine
			ship_radar
			ship_sonar
		}
	}

	mine_sweeper_light_late_frigate = { # improved variant for late war

		priority = {
			factor = 1
		}

		enable = {
			has_tech = basic_dp_light_battery
			has_tech = improved_naval_mines
		}

		role_icon_index = 5

		target_variant = {
			match_value = 1100.0

			type = mdg_ship_hull_frigate
			modules = {
				rear_1_custom_slot = ship_mine_sweeper_1
				mid_1_custom_slot = ship_mine_sweeper_1
				fixed_ship_battery_slot = {
					any_of = { mdg_ship_light_cannon mdg_ship_light_cannon_dp }
					upgrade > current
				}
				fixed_ship_engine_slot = {
					any_of = { light_ship_engine }
					upgrade > current
				}
				fixed_ship_torpedo_slot = empty
				fixed_ship_anti_air_slot = {
					any_of = { ship_anti_air }
					upgrade > current
				}
			}
		}

		requirements = {
			module = ship_mine_sweeper_1
		}
		allowed_modules = {
			ship_mine_sweeper_1
			ship_mine_warfare
			ship_anti_air
			mdg_ship_light_cannon
			mdg_ship_light_cannon_dp
			ship_depth_charge
			ship_fire_control_system
			light_ship_engine
			ship_radar
			ship_sonar
		}
	}
}

GER_naval_mine_layer_frigate = {
	category = naval

	blocked_for = {
	}

	available_for = {
		GER
	}

	roles = {
		naval_mine_layer_frigate
	}

	priority = {
		factor = 5
		modifier = {
			factor = 5
			has_war = yes
		}
	}

	mine_layer_light_frigate = {

		priority = {
			factor = 2
		}

		role_icon_index = 4

		target_variant = {
			match_value = 1000.0

			type = mdg_ship_hull_frigate
			modules = {
				rear_1_custom_slot = ship_mine_layer_1
				mid_1_custom_slot = ship_mine_layer_1
				fixed_ship_engine_slot = {
					any_of = { light_ship_engine }
					upgrade > current
				}
				fixed_ship_battery_slot = {
					any_of = { mdg_ship_light_cannon }
					upgrade > current
				}
				fixed_ship_torpedo_slot = empty
				fixed_ship_fire_control_system_slot = {
					any_of = { ship_fire_control_system ship_radar }
					upgrade > current
				}
				fixed_ship_radar_slot = {
					any_of = { ship_radar }
					upgrade > current
				}
				fixed_ship_anti_air_slot = empty
			}
		}

		requirements = {
			module = ship_mine_layer_1
		}
		allowed_modules = {
			ship_mine_layer_1
			ship_anti_air
			mdg_ship_light_cannon
			mdg_ship_light_cannon_dp
			ship_fire_control_system
			light_ship_engine
			ship_torpedo
			ship_radar
			ship_sonar
		}
	}
}
