mdg_macs = {
	category = naval
	
	blocked_for = {
	}
	
	available_for = {
	}
	
	roles = {
		mdg_naval_mac
	}

	mdg_merchant_carrier_default = {

		priority = {
			factor = 5
			modifier = {
				factor = 10
				is_major = no
			}
		}

		role_icon_index = 7
		
		history = yes
		target_variant = {
			match_value = 1000.0
			
			type = mdg_ship_hull_mac
			modules = {
				fixed_ship_deck_slot_1 = ship_escort_deck_space
				fixed_ship_anti_air_slot = { any_of = { ship_anti_air_1 } upgrade > current }
				fixed_ship_engine_slot = carrier_ship_engine_1
				fixed_ship_secondaries_slot = { any_of = { mdg_ship_light_cannon_1 } upgrade > current }
			}
		}
		
		allowed_modules = {
			mdg_ship_light_cannon
			mdg_ship_light_cannon_dp
			ship_anti_air
			carrier_ship_engine
			ship_radar
			ship_escort_deck_space
		}
	}
}
